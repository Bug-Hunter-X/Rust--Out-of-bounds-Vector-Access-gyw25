fn main() {    let mut vec = Vec::new();    vec.push(1);    vec.push(2);    vec.push(3);    let index = 1;    if index < vec.len() {        println!("Value at index {}: {}", index, vec[index]);    } else {        println!("Index out of bounds");    }    // safer alternative using get()    match vec.get(10) {        Some(value) => println!("Value at index 10: {}", value),        None => println!("Index 10 is out of bounds"),    }}